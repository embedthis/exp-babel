{
    "name": "exp-babel",
    "description": "ES6 Compile for Expansive using babel",
    "version": "0.2.1",
    "keywords": [
        "expansive-plugin",
        "babel"
    ],
    "licenses": "GPL",
    "repository": {
        "type": "git",
        "url": "git://github.com/embedthis/exp-65.git"
    },
    "engines": {
        "node": ">= 0.4.0"
    },
    "scripts": {
        "postcache": {
            "script": "if (!Cmd.locate('babel')) { if (!Cmd.locate('npm')) { throw 'Cannot locate the \"npm\" utility.' } else { run('npm install -g babel')}}"
        }
    },
    "pak": {
        "version": ">=0.10"
    }
}
